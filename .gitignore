# dependencies
node_modules/

# python
.venv/
__pycache__/
*.pyc

# env / secrets
.env
.env.local
.env.*.local

# build output
dist/
build/
*.tsbuildinfo

# terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log

# localstack / docker volumes
local/volume/
.localstack/

# os / editor
.DS_Store
Thumbs.db

# claude code local (machine-specific)
.claude/settings.local.json

# local dev notes / scratch memos (not part of the repo's public purpose)
docs_local/

# dotfiles for local dev tools (not part of the repo's public purpose)
.agents/
.claude/
.codex/
.vscode/

# local reference prototype (not part of the repo's public purpose)
aws-quiz-v2.tsx
