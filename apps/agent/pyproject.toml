[tool.pytest.ini_options]
# tests/ から quiz_agent パッケージ(リポジトリ非インストール)をimportするため
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]
