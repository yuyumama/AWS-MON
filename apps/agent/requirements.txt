# CacheConfig(プロンプトキャッシュ)を使うため 1.45 以上
strands-agents>=1.45
pydantic>=2.0
python-dotenv>=1.0.0
# AWSドキュメントMCP(フェーズ2-4)。strandsのMCPClient(stdio)から起動する
mcp>=1.0
awslabs.aws-documentation-mcp-server>=1.1.26
# オブザーバビリティ(フェーズ3-1)。ADOT SDKでCloudWatch OTLPエンドポイントへ直接送信する。
# scripts/run_server_otel.sh から opentelemetry-instrument 経由で起動したときだけ有効。
aws-opentelemetry-distro>=0.10.0
# Guardrails ApplyGuardrail 呼び出し(フェーズ3-3)
boto3>=1.43.51
